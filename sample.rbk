// input key name on the left, output key name on the right
// keyname -> keyname
// The below hotkey sets scroll lock to left alt
Scroll_Lock -> Alt_L

// Run `xmodmap -pke` to see a full list of keycodes and key names (only the key names on the right side are relevant)